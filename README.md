# Dotfiles

```
cd
git clone git@github.com:johnmdonahue/dotfiles.git
cd dotfiles
echo "source ~/dotfiles/zshrc" >> ~/.zshrc
echo "source ~/dotfiles/vimrc" >> ~/.vimrc
open ~/dotfiles/custom.terminal
```
